// Test generated by RoostGPT for test React_Test using AI Type Open AI and AI Model gpt-4

import React from "react";
import { render } from "@testing-library/react";
import App from "./App";
import Login from "./components/Login";

jest.mock("./components/Login", () => {
  return function DummyLogin() {
    return (
      <div data-testid="child">Dummy Login Component</div>
    );
  };
});

describe("App Component", () => {
  let component;

  beforeEach(() => {
    component = render(<App />);
  });

  test("renders without crashing", () => {
    expect(component).toBeTruthy();
  });

  test("renders header correctly", () => {
    const { getByText } = component;
    const headerElement = getByText(/This is React Testing Tutorial/i);
    expect(headerElement).toBeInTheDocument();
  });

  test("renders Login component", () => {
    const { getByTestId } = component;
    const loginElement = getByTestId('child');
    expect(loginElement).toBeInTheDocument();
  });
});
