// Test generated by RoostGPT for test React_Test using AI Type Open AI and AI Model gpt-4

import { validateEmail } from './Login';

describe("Email Validation", () => {
  test("should validate a correct email", () => {
    const email = "test@email.com";
    const result = validateEmail(email);
    expect(result).toBe(true);
  });

  test("should not validate an incorrect email", () => {
    const email = "testemail.com";
    const result = validateEmail(email);
    expect(result).toBe(false);
  });

  test("should not validate an empty email", () => {
    const email = "";
    const result = validateEmail(email);
    expect(result).toBe(false);
  });

  test("should not validate an email without @ symbol", () => {
    const email = "testemail.com";
    const result = validateEmail(email);
    expect(result).toBe(false);
  });

  test("should not validate an email without domain", () => {
    const email = "test@.com";
    const result = validateEmail(email);
    expect(result).toBe(false);
  });

  test("should not validate an email without . symbol", () => {
    const email = "testemailcom";
    const result = validateEmail(email);
    expect(result).toBe(false);
  });
});
