// Test generated by RoostGPT for test React_Test using AI Type Open AI and AI Model gpt-4

import { reportWebVitals } from './reportWebVitals';

jest.mock('web-vitals', () => ({
  getCLS: jest.fn(),
  getFID: jest.fn(),
  getFCP: jest.fn(),
  getLCP: jest.fn(),
  getTTFB: jest.fn(),
}));

describe('reportWebVitals', () => {
  let onPerfEntry;

  beforeEach(() => {
    jest.clearAllMocks();
    onPerfEntry = jest.fn();
  });

  it('should call the onPerfEntry function if it is provided', async () => {
    await reportWebVitals(onPerfEntry);

    expect(onPerfEntry).toHaveBeenCalled();
  });

  it('should not call the onPerfEntry function if it is not provided', async () => {
    await reportWebVitals();

    expect(onPerfEntry).not.toHaveBeenCalled();
  });

  it('should call the web-vitals methods with the onPerfEntry function', async () => {
    const { getCLS, getFID, getFCP, getLCP, getTTFB } = require('web-vitals');

    await reportWebVitals(onPerfEntry);

    expect(getCLS).toHaveBeenCalledWith(onPerfEntry);
    expect(getFID).toHaveBeenCalledWith(onPerfEntry);
    expect(getFCP).toHaveBeenCalledWith(onPerfEntry);
    expect(getLCP).toHaveBeenCalledWith(onPerfEntry);
    expect(getTTFB).toHaveBeenCalledWith(onPerfEntry);
  });
});
